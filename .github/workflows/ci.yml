name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Check formatting
        shell: bash
        run: |
          set -euo pipefail
          files="$(git ls-files '*.go')"
          if [ -z "${files}" ]; then
            exit 0
          fi
          unformatted="$(gofmt -l ${files})"
          if [ -n "${unformatted}" ]; then
            echo "These files need gofmt:"
            echo "${unformatted}"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./...

      - name: Build default CLI
        shell: bash
        run: |
          set -euo pipefail
          make clean build
          ./tmux-connect daemon help | grep -F "Compiled platforms:"
          ./tmux-connect daemon help | grep -F "telegram, feishu, slack, discord, whatsapp"
          ./tmux-connect daemon run --help 2>&1 | grep -F 'remote platform in this build (telegram|feishu|slack|discord|whatsapp)'

      - name: Build selective CLI
        shell: bash
        run: |
          set -euo pipefail
          make clean build EXCLUDE=feishu,whatsapp
          ./tmux-connect daemon help | grep -F "Compiled platforms:"
          ./tmux-connect daemon help | grep -F "telegram, slack, discord"
          ./tmux-connect daemon run --help 2>&1 | grep -F 'remote platform in this build (telegram|slack|discord)'
